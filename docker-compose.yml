version: '3.8'
services:
  stable-diffusion:
    image: stable-diffusion
    volumes:
      - .:/app
      - ./data:/app/data
    entrypoint: ["/app/entrypoint.sh"]
    command: python /app/scripts/txt2img.py --prompt "A futuristic cityscape" --output_dir /app/data/output
